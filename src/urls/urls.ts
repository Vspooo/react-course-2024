const baseURL = "http://owu.linkpc.net/carsAPI/v2"
const auth = "/auth"
const register = "/users"
const user = "/auth/me"
const cars = "/cars"
const urls = {
    baseURL,
    auth,
    register,
    user,
    cars
}

export {
    urls
}