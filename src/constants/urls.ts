
const baseURL = "https://jsonplaceholder.typicode.com"
const users = "/users"
const posts = "/posts"
const comments = "/comments"
const urls = {
    baseURL,
    users,
    posts,
    comments
}

export {
    urls
}