const baseURL = "http://owu.linkpc.net/carsAPI/v2"
const auth = "/auth"
const refresh = "/auth/refresh"
const register = "/users"
const user = "/auth/me"
const cars = "/cars"
const urls = {
    baseURL,
    auth,
    refresh,
    register,
    user,
    cars
}

export {
    urls
}